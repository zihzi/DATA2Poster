import altair as alt
import pandas as pd
import json
<imports>
def plot(data: pd.Dataframe):
    # If data need to be transformed (ex.Calculate the mean, Filter the data, Group the data), trnasform data FIRST.

    <stub> # only modify this section

    return chart
chart = plot(data) Always include this line. No additional code beyond this line.