{
  "title": "Question_for_Insights",
  "type": "object",
  "description": "Contains of question and its action based on a data patttern for further analysis.",
  "properties": {
    "chosen_pattern": {
      "type": "string",
      "description": "The first data patterns."
    },
    "query_object": {
      "type": "object",
      "description": "One question and its action for further analysis.",
      "properties": {
        "question": {
          "type": "string",
          "description": "The question."
        },
        "action": {
          "type": "string",
          "description": "The description of how to solve the question using visualization."
        }
      },
      "required": [
        "question",
        "action"
      ]
    }
  },
  "required": [
    "chosen_pattern",
    "query_object"
  ]
}